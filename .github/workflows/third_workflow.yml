name: Second Workflow
on: [push]

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display node version on Ubuntu
        run: | # путь к каталогу и что лежит в каталоге
          pwd 
          ls -a 
